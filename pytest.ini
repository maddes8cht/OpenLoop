[pytest]
testpaths = tests
markers =
    tier1: Integration — module integration with a mock runner (no opencode)
    tier2: Integration — system integration, requires `opencode` in PATH
    tier3: Integration — artifact verification
addopts = -ra
